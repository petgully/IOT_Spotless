# Force LF line endings on shell scripts and unit files so they work
# correctly on Linux/Raspberry Pi when the repo is edited from Windows.
*.sh         text eol=lf
*.bash       text eol=lf
*.service    text eol=lf
.env*        text eol=lf

# Default to native line endings for everything else
* text=auto
