# =============================================================================
# Python Dependencies for Project Spotless - Raspberry Pi Master Controller
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# MQTT Client
paho-mqtt>=1.6.0

# GPIO Control for Raspberry Pi 5 (direct GPIO relays)
# Pi 5 / Bookworm uses libgpiod 2.x. Install on the Pi with:
#   sudo apt install python3-libgpiod
# Or via pip (inside the venv): pip install "gpiod>=2.1"
# NOTE: gpio_controller.py also supports the older 1.x API as a fallback.
gpiod>=2.1.0

# Web Kiosk (Flask + WebSocket)
flask>=2.3.0
flask-socketio>=5.3.0
python-socketio>=5.8.0
python-engineio>=4.5.0

# Database connectivity (AWS RDS Aurora MySQL)
pymysql>=1.1.0
cryptography>=3.4.0    # For SSL connections

# Environment variables (for database credentials, admin password, API keys)
python-dotenv>=1.0.0

# JSON handling (standard library)
# json is built-in

# Logging (standard library)
# logging is built-in

# HTTP requests
requests>=2.28.0
